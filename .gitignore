# Private source spreadsheet — never publish this.
# data/resources.csv is the published copy; this original stays local.
ListofResources.xlsx
*.xlsx

# Excel / Office lock files
~$*
.~lock.*

# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Editors / tooling
.vscode/
.idea/
*.swp
.claude/

# Stale hand-renamed build from 2026-08-09. It predates the no-JavaScript
# fallback and the mobile fixes, so it hangs on "Loading…" in some viewers.
# Delete it, or regenerate it from dist/resources-app.html, then remove this line.
dist/PolicyMenu.html

# Python
__pycache__/
*.py[cod]

# Local source copy of the header logo — img/isee-logo.png is the tracked
# asset the site actually references.
/ISEENAC.png
